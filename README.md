# Основы Python

Учебный репозиторий, в котором я последовательно изучаю Python и применяю полученные знания в практических задачах.

Основное внимание уделяется написанию понятного кода, работе с командной строкой, тестированию и созданию небольших прикладных инструментов.

## Пройденные темы

### Дни 1–4 — основы языка

- переменные;
- ввод и вывод данных;
- условия;
- циклы `while` и `for`;
- списки;
- индексы;
- вложенные циклы;
- проверка пользовательского ввода;
- учебные проекты: калькулятор, менеджер студентов и менеджер оценок.

### Дни 5–8 — функции, словари и файлы

- функции;
- параметры и аргументы;
- `return`;
- обработка ошибок через `try / except`;
- словари;
- списки словарей;
- вложенные структуры данных;
- чтение и запись файлов;
- создание текстовых отчётов.

### Дни 9–11 — анализ данных и логов

- обработка строк;
- анализ логов;
- уровни логирования;
- подсчёт событий;
- формирование сводных отчётов;
- проверка состояний сервисов;
- разбор структурированных логов.

### Дни 12–16 — разработка CLI-приложения

Создано консольное приложение для управления состояниями сервисов.

Реализовано:

- обработка аргументов командной строки;
- команды `list`, `status`, `start` и `stop`;
- хранение состояния сервисов в JSON;
- загрузка и сохранение данных;
- разделение программы на функции;
- функция `main()`;
- обработка отсутствующего или повреждённого JSON-файла;
- восстановление стандартного состояния сервисов.

### Дни 17–19 — тестирование

- базовые проверки через `assert`;
- тестовые функции;
- регрессионное тестирование;
- установка и использование `pytest`;
- автоматический поиск тестов;
- pytest fixtures;
- подготовка тестовых данных;
- устранение повторяющегося кода в тестах.

### День 20 — логирование

- модуль `logging`;
- уровни логирования;
- запись логов в файл;
- сообщения `INFO`, `WARNING` и `ERROR`;
- интеграция логирования в CLI-приложение.

### День 21 — argparse

- модуль `argparse`;
- `ArgumentParser`;
- позиционные аргументы;
- `parse_args()`;
- объект `Namespace`;
- `choices`;
- необязательные аргументы;
- обработка ошибок CLI;
- тестирование парсера через `pytest`.

### День 22 — исключения

- обработка исключений;
- пользовательские сообщения об ошибках;
- предотвращение аварийного завершения программы;
- защитное программирование.

### День 23 - 24 — файлы и процессы Linux

- навигация по файловой системе;
- создание, копирование и перемещение файлов;
- перенаправление вывода через `>` и `>>`;
- поиск через `grep` и `find`;
- права доступа и `chmod`;
- просмотр процессов;
- фоновые и активные задачи;
- завершение процессов.

### Day 25 — GitHub Actions

- Создан workflow GitHub Actions
- Настроен автоматический запуск pytest при push и pull request
- Тесты запускаются в окружении Ubuntu
- Настроены actions/checkout и actions/setup-python
- CI успешно проходит

### Day 26 — Зависимости проекта

- Создан файл requirements.txt
- Зафиксирована версия pytest
- Создано виртуальное окружение .venv
- Настроена установка зависимостей через pip
- GitHub Actions использует requirements.txt

## Основной учебный проект

Консольное приложение для управления сервисами:

```bash
python3 service_cli.py list
python3 service_cli.py status nginx
python3 service_cli.py start nginx
python3 service_cli.py stop nginx