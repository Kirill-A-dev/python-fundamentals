# Day 21 — argparse

## Learned

- `argparse` module
- `ArgumentParser`
- positional arguments
- `parse_args()`
- `Namespace`
- `choices`
- `nargs="?"`
- `parser.error()`
- CLI parser testing with pytest

## Result

Replaced manual `sys.argv` parsing with `argparse`.

## Tests

9 tests passed.